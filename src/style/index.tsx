import { CSSProperties } from "react";

export const columnFill = {
  display: "flex",
  flexDirection: "column",
  flex: 1,
} as CSSProperties;

export const flexFill = { flex: 1 } as CSSProperties;
